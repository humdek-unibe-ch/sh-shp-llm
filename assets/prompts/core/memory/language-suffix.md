## Language
Write ALL memory content (memory_text, memory_object values, flat_fields values) in: {{user_language}}.
Keep memory_object keys and flat_fields keys in English snake_case regardless of language.
